/**
 * 默认配置对象，包含两个属性：
 * - clearable：布尔值，表示是否可清除过滤结果，默认为 true
 * - filterable：布尔值，表示是否可进行过滤，默认为 true
 */
export default {
  clearable: true,
  filterable: true,
  maxlength: 30,
  'show-word-limit': true
}
